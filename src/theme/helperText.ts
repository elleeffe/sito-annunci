import {Components} from '@mui/material';

export const formHelperText: Components['MuiFormHelperText'] = {
  styleOverrides: {
    root: {
      position: 'absolute',
      bottom: '-20px',
      left: 0,
    },
  },
};
